# 仓库工作规则

## 本机配置

处理本仓库中的任何任务前，如果 `.machine.local.yaml` 存在，必须先读取它。该文件是当前机器角色和能力的本地事实源，已被 Git 忽略。

如果该文件不存在，不得自行推断当前机器是实验机。在用户从 `environments/profiles/` 中选择配置模板之前，只能检查仓库和维护文档。

## 角色行为

### `learning`（学习机）

- 可以进行阅读、讲解、知识笔记、架构设计、评审、规划和学习日志维护。
- 不得安装依赖、配置操作系统、启动服务、运行工程实验或压测、下载模型，也不得实现文档以外的源代码。
- 当每日任务同时包含知识学习和工程实验时，只完成当前机器支持的知识部分；实验专属验收项保持原状，留给实验机完成。
- 直接跳过当前机器不支持的步骤，不要反复说明这些步骤无法执行。只有当机器能力限制阻塞整个用户请求、用户询问状态，或共享日志需要准确记录证据时，才说明该边界。
- 不得仅凭文档内容或模拟输出，将实验专属验收项标记为通过。

### `experiment`（实验机）

- 在用户请求和常规安全规则范围内，可以执行文档维护、知识学习、源代码实现、环境配置、服务运行、工程实验、压测和证据采集。
- 当前任务涉及学习机遗留的待完成实验项时，应继续完成这些验收项。
- 保存实验记录时，必须同时记录机器 ID 和 Git commit。

## 共享状态规则

- 当前执行入口是 `docs/ai-infra-landscape-orientation.md`，原 32 周逐日计划及其进度文件暂停使用。
- 全景阶段不创建每日学习日志或复杂任务状态，只在当前方案中维护最小认知记录和方向选择备忘录。
- 不得为两台机器分别复制学习计划、源代码或学习记录。
- 机器专属的环境基线和证据应保存在 `environments/`，或被 Git 忽略的本地目录、产物目录中。
- 本机配置文件 `.machine.local.yaml` 绝对不能提交到 Git。
