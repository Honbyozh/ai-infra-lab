# AI Infra 学习方案版本对照

三个版本的定位、差异与选型理由。用于回看决策依据，不替代各版本正文。

- 初版：`ai-infra-gated-learning-plan.md`
- v2：`ai-infra-gated-learning-plan-v2.md`
- v3：`ai-infra-gated-learning-plan-v3.md`（当前推荐执行底稿）

## 1. 一句话定位

| 版本 | 定位 | 适合场景 |
|---|---|---|
| 初版 | 严格依赖 + 全有或全无验收 | 个人学习，心智负担较低 |
| v2 | 在初版上加一套可校验的工作流状态机 | 多 Agent / 长期严格执行 |
| v3 | 初版为底稿 + 只吸收 v2 的外部锚点 | 个人实际学习（推荐） |

## 2. 核心维度对照

| 维度 | 初版 | v2 | v3 |
|---|---|---|---|
| 篇幅 | 424 行 | 523 行 | ~250 行 |
| 状态数 | 6 种 | 7 种（含 `passed_with_reinforcement`） | 4 种 |
| 验收模型 | 所有验收通过才完成 | 硬验收/软验收 + 补强任务 | 必须/可选（仅分类，无状态机） |
| 外部校准 | 主要后期引入 | S1 即要求真实模型配置对照 | 保留 S1 外部锚点 |
| 阻塞处理 | 任一验收失败可能卡住 | 可带补强任务继续 | 可选项不阻塞；必须项仍严格 |
| 进度管理 | 人工维护 | `progress.yaml` + `plan_check.py` | 一张 Markdown 表，两条规则 |
| 每任务字段 | 7 项 | 7 项 + H/S 标注 | 4 项（目标/必须/可选/证据） |
| 证据体系 | 多级等级表 | 多级等级表 | 压成一句排序直觉 |
| 维护成本 | 较高 | 很高 | 低 |
| 总体判断 | 更实用 | 更严谨但过度工程化 | 实用 + 补上初版最大短板 |

## 3. 各版本关键得失

### 初版
- 优点：心智负担低（6 状态、线性规则）；任务内容表达完整；依赖严格防跳级；单人够用。
- 缺点：全有或全无易锁死（次要验收也阻塞）；自我验收过多（FND-08/09 缺客观对照）；文档工件太多；人工状态易失真。

### v2
- 优点：**外部答案锚点**（唯一确实值钱）；区分关键/非关键验收的思想正确；降低单点阻塞；状态一致性可自动校验。
- 缺点：为解决流程问题又造一套更大流程；补强机制把早期阻塞平移成后期债务；`passed_with_reinforcement` 语义模糊；软硬划分主观；**工具只能校验流程、不能校验能力**；脚本随规则变更需同步维护；可读性下降；与"减少文档维护"目标相悖。

### v3
- 取舍：留初版依赖 + 知识/实验分离 + 真实集成门禁；吸收 v2 外部锚点；砍掉补强状态机、YAML、校验脚本、量级/会话管理、多级证据表、冗余文档产物。
- 关键决定：H/S 的**分类思想保留、围绕它的状态机砍掉** —— 可选项不阻塞，但要求备注"没做什么、为什么"。
- 已知未解：S1 整体认知负荷仍偏大（11 任务四条腿），属于是否重构 S1 定位的方向决策，留待全景方案结论后再定。

## 4. 三者共同问题（v3 部分缓解）

| 共同问题 | 初版 | v2 | v3 处理 |
|---|---|---|---|
| 过早锁定"分布式推理"主线 | 有 | 有 | 顶部加前置声明：需全景方案先确认方向 |
| 验收与文档绑定太紧 | 重 | 更重 | 任务卡砍到 4 项，产物大幅精简 |
| S1 实际工作量大于"11 个任务"直觉 | 有 | 有 | 未解，待方向确认后再重构 |
| 易优化流程而非优化学习 | 中 | 高 | 砍流程，把重心移回验收证据本身 |

## 5. 选型建议

- 个人学习、实际执行：**v3**。
- 多 Agent 协作、需要机器可校验的状态一致性：v2。
- 想要最低理解成本的线性流程、且能接受偶发锁死：初版。
- 前提：任何版本执行前，先用独立的全景认知方案确认"分布式推理"确为候选主线。
