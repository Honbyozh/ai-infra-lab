# AI Infra 能力门禁学习方案 v3

> 本版是 v1（初版）与 v2 对比后的融合精简稿，取向：**个人实际学习**。
> 设计原则：验收由证据决定而非时间；保留严格依赖与真实集成门禁；吸收 v2 唯一确实值钱的改进——外部答案锚点；砍掉所有过度工程化的流程机制。
>
> **相对 v2 删除**：`passed_with_reinforcement`、补强任务/债务/清偿系统、`progress.yaml`、`plan_check.py`、会话次数与量级预估管理、多级证据等级说明、每任务大量固定文档产物。
> **相对 v1 改进**：每个任务验收拆成「必须 / 可选」两档（只是区分，不引入状态机）；FND-08、FND-09 引入真实模型配置外部对照；状态精简为 4 种。
>
> **前置声明**：本方案已经预设了"高性能分布式推理"主线。是否采用应先由独立的全景认知方案确认，本文件只在方向确定后作为执行底稿。

## 1. 定位

目标不是泛化的 AI Platform 或 MLOps，而是形成一条纵向能力链：

```text
模型推理语义
  -> 推理 Runtime 与性能工程
  -> KV Cache、批处理、内存和调度
  -> GPU Kernel、Runtime 与编译器
  -> 多 GPU 并行、NCCL 与高速网络
  -> 集群级容量、可靠性和 cost-to-serve
```

目标角色：**高性能分布式推理系统工程师，并具备向 GPU Runtime / Kernel 下钻的能力。**

贯穿全程的项目统一命名为 `Distributed Inference Lab`——用于复现、测量、分析和扩展真实推理引擎的实验工作台，不以重写生产级 vLLM 为目标。

工作量分配建议：约 30% 最小自研实现建立机制理解；约 50% 阅读/修改/扩展 vLLM、SGLang、PyTorch、Triton 等真实项目；约 20% 基准、性能报告、故障分析与外部交流。

## 2. 进度由验收决定，不设固定周期

不规定周数、日期、每日时长。判定原则：

- 阅读结束、投入时间、文档总结，都不代表完成；
- 计划运行结果、模拟输出、缺少原始数据的性能结论，均不算证据；
- 任务的全部**必须验收**通过后，状态才可变为「完成」；
- 前置任务全部完成后，后继任务才从「未开始」变为可执行。

**可选验收不阻塞**：可选项未完成不影响任务判定为完成，但应在任务记录里留一句"哪些可选项没做、为什么"，避免默默丢失。

连续三次尝试没有新增证据时，标记为「阻塞」并写下阻塞诊断，不要继续堆时间。同时进行的实验任务不超过 2 个。

## 3. 状态与证据（精简）

### 3.1 四种状态

| 状态 | 含义 |
|---|---|
| 未开始 | 前置未全部完成，或尚未动手 |
| 进行中 | 已有有效工作记录，未通过全部必须验收 |
| 阻塞 | 多次尝试无新增证据，或存在明确外部阻塞 |
| 完成 | 所有必须验收均有可核查证据 |

不引入债务、补强、部分完成等中间态。次要缺口用一句备注记录，不建独立状态。

### 3.2 什么算证据

按可信度从强到弱：自动化正确性测试与原始性能数据 > 可复现命令 / Profiler 输出 / 故障日志 > 源码与 Git diff > 公式推导 / 架构图 / 注释 > 文字解释与自测。

性能结论必须带：环境、变量、预热方法、重复运行结果、原始数据、统计方式；不得只保留最好结果。

不再维护多级证据"等级表"；上面这一句排序作为直觉即可。

### 3.3 外部答案锚点（源自 v2，保留）

凡涉及可被客观核对的推导（shape、显存、KV Cache、FLOPs），验收必须包含**至少一个外部对照**，而不是纯自证：

- 用真实开源模型的公开 `config.json`（`num_hidden_layers`、`num_attention_heads`、`num_key_value_heads`、`hidden_size`、`head_dim`、`torch_dtype` 等）反推数值；
- 把手算结果与"权重文件实际大小 / 官方模型卡显存要求 / 框架实际占用"对齐，误差落在可解释范围并说明来源。

这是 v2 唯一确实降低"自己推导、自己宣布正确"风险的机制，故保留；其余 v2 流程一律不引入。

### 3.4 累计复验

「完成」表示当时有充分证据，不代表能力永久有效。后续阶段的集成门禁会再次要求用早期能力（重估显存、解释 Roofline、拆端到端延迟）。复验不过时，不抹历史证据，回到对应任务补做即可——不建独立的债务追踪表。

## 4. 双机与云实验模式

每台机器用被 Git 忽略的 `.machine.local.yaml` 声明 `machine_id`、`role` 和能力。

- 模式 A：一台学习机 + 一台实验机；
- 模式 B：两台本地机器都当学习机，按需租 CPU/GPU 云服务器作临时实验机。

学习机负责阅读、推导、源码分析、设计、自测、文档；实验机负责实现、编译、运行、Profiler、基准、硬件实验。学习机不得用文字总结代替真实实验验收。

实验任务需声明所需能力标签，避免把所有实验机当同一种资源：

| 能力标签 | 含义 |
|---|---|
| `linux_exec` | 可运行 Linux 命令并保存原始输出 |
| `pytorch_cpu` | 可运行/修改 PyTorch CPU 程序 |
| `cpp_build` | 可编译、测试、调试 C++ |
| `single_gpu` | 单张可用 GPU |
| `cuda_build` | 可构建 CUDA/Triton 实验 |
| `gpu_profiler` | 可运行相应 GPU Profiler |
| `multi_gpu` | 单机多 GPU |
| `nvlink` | 可验证 NVLink/NVSwitch 行为 |
| `multi_node` | 至少两个可通信计算节点 |
| `rdma` | 可进行 RDMA/InfiniBand 实验 |

实验记录须含 machine ID、Git commit、软硬件环境、完整命令、原始结果、失败记录。全仓库只维护一份进度记录（一个 Markdown 清单足够，见 §7），不按机器复制路线。

## 5. 总体阶段路线

后续阶段只在前一阶段完成后展开。展开下一阶段本身是一项任务，依据已有证据、当时硬件与上游项目状态制定。

- **S1 最小必要基础**：进入真实推理系统的最低通行能力，详见 §6。退出后解锁 S2 规划。
- **S2 单 GPU 推理系统**：真实引擎执行路径、continuous batching、KV/prefix cache、内存管理、量化、调度、端到端性能分析。退出标准：能修改真实引擎的非平凡执行路径，用正确性测试+原始数据说明影响，并完成一次端到端瓶颈分析。环境 `single_gpu`、`gpu_profiler`。
- **S3 GPU 性能工程与 CUDA/Triton**：GPU 执行/内存模型、Roofline、访存合并、共享内存、Tensor Core、算子融合、编译器与 Runtime。退出标准：至少一个推理关键算子通过正确性测试+Profiler 分析+可靠基线对比；能解释成功优化与失败优化各一次。环境 `single_gpu`、`cuda_build`、`gpu_profiler`。
- **S4 分布式推理与高速通信**：TP/PP/EP/MoE、NCCL、NVLink、RDMA、拓扑、通信计算重叠、Prefill/Decode 分离、KV Cache 传输。退出标准：完成真实多 GPU/多节点实验，给出扩展效率、通信占比、瓶颈证据与失败边界，不能只展示吞吐增长。环境 `multi_gpu`（按需 `nvlink`/`multi_node`/`rdma`）。
- **S5 集群级调度、可靠性与成本**：拓扑感知调度、异构 GPU、路由、容量规划、弹性、故障恢复、可观测性、cost-to-serve。退出标准：从请求/模型/Runtime/节点/fleet 多层解释一个真实瓶颈或故障，并用数据给出容量或成本决策。
- **S6 外部验证与行业级作品集**：上游贡献、公开复现、性能报告、故障复盘、跨硬件迁移、技术表达。退出标准至少含：一个被上游接受的实质贡献、两份可复现性能报告、一个公开故障分析、一次跨后端/跨硬件迁移分析，以及对整条链路的完整讲解。

## 6. S1 任务图与任务卡

S1 共 11 个原子任务，知识任务与实验任务分离：学习机可推进理论，同时阶段最终状态不会在没有真实执行证据时通过。

```text
FND-01 推理系统全景 [可执行]
  +-> FND-02 Linux 执行与内存模型
  |     +-> FND-03 Linux 观察实验
  |     +-> FND-06 最小 C++ 系统素养
  |             +-> FND-07 C++ 构建与调试实验
  |
  +-> FND-04 Tensor 与 PyTorch 语义
        +-> FND-05 Tensor 行为实验
        +-> FND-08 Transformer 推理与 KV Cache
                +-> FND-09 容量与性能模型 <- FND-02
                        +-> FND-10 基准方法与诊断设计

FND-03 + FND-05 + FND-07 + FND-08 + FND-09 + FND-10
  -> FND-11 S1 集成门禁 -> PLAN-S2
```

每张任务卡只保留四项：**目标 / 必须验收 / 可选扩展 / 证据**。

---

### FND-01 推理系统全景
- 类型 knowledge｜前置 无
- **目标**：建立从请求到 token 输出的最小端到端心智模型。
- **必须验收**：按顺序解释请求→tokenization→batching→forward→sampling→streaming；在链路图上标出 CPU/GPU/内存/网络/存储的参与位置。
- **可选扩展**：区分训练/离线推理/在线推理；写出三个当前不能证明的性能假设。
- **证据**：链路图 + 脱稿自测记录。解锁 FND-02、FND-04。

### FND-02 Linux 执行与内存模型
- 类型 knowledge｜前置 FND-01
- **目标**：建立定位 CPU/内存/IO 问题所需的系统模型。
- **必须验收**：解释进程/线程/用户态/内核态边界；解释 runnable/running/sleeping/上下文切换/load average；区分虚拟内存/RSS/page cache/swap/文件映射；对"高 load 低 CPU""高 CPU 低吞吐""高 IO wait"三种现象各给出证据收集顺序。
- **可选扩展**：解释 cache locality、NUMA locality、内存带宽为何影响推理。
- **证据**：关系图 + 诊断决策表。解锁 FND-03、FND-06，满足 FND-09 一个前置。

### FND-03 Linux 观察实验
- 类型 experiment｜前置 FND-02｜能力 `linux_exec`
- **目标**：把系统概念映射到真实进程与原始指标。
- **必须验收**：采集目标进程 PID/线程/状态/CPU/RSS/上下文切换/fd/IO 证据；制造至少一种 CPU/内存/IO 行为并观察指标变化；从新终端按记录复现主要结果。
- **可选扩展**：对观察给出解释，并指出指标不能证明什么。
- **证据**：完整命令 + 原始输出 + machine ID + Git commit + 环境。满足 FND-11 一个前置。

### FND-04 Tensor 与 PyTorch 语义
- 类型 knowledge｜前置 FND-01
- **目标**：能沿推理程序跟踪 tensor 的语义与潜在内存行为。
- **必须验收**：对给定操作逐步推导 shape/dtype/device；据 stride 判断 tensor 是否连续；区分 view/reshape/transpose/contiguous/clone 的行为。
- **可选扩展**：解释 dtype 转换与 device 传输的成本；阅读一个最小 forward 并标注主要中间 tensor。
- **证据**：手工推导 + 带注释的最小程序阅读记录。解锁 FND-05、FND-08。

### FND-05 Tensor 行为实验
- 类型 experiment｜前置 FND-04｜能力 `pytorch_cpu`
- **目标**：用运行结果验证 shape/stride/view-copy/dtype 判断。
- **必须验收**：实际打印并核对 shape/stride/dtype/device/data pointer 或 storage 关系；构造至少一个非连续 tensor 并验证哪些操作共享存储/复制/失败；用断言把预期行为固化为可重复测试。
- **可选扩展**：比较至少两种 dtype 的内存占用。
- **证据**：源码 + 测试输出 + machine ID + Git commit + 环境。满足 FND-11 一个前置。

### FND-06 最小 C++ 系统素养
- 类型 knowledge｜前置 FND-02
- **目标**：具备阅读推理引擎底层代码所需的最低 C++ 语言与构建心智模型。
- **必须验收**：区分栈/堆/值语义/引用/指针/对象生命周期；解释 RAII、`unique_ptr`、`shared_ptr`、移动语义解决的问题；解释头文件/翻译单元/编译/链接/动态库的关系。
- **可选扩展**：阅读含类、模板容器、资源管理的小程序并描述执行路径；明确高级模板元编程不在本阶段。
- **证据**：代码注释 + 构建链路图。解锁 FND-07。

### FND-07 C++ 构建与调试实验
- 类型 experiment｜前置 FND-06｜能力 `cpp_build`
- **目标**：验证最低限度的 C++ 修改/构建/测试/调试能力。
- **必须验收**：修改一个含资源生命周期或内存访问的小程序；从干净构建目录完成编译链接；运行自动化测试；用调试器或 sanitizer 定位并修复一个预置问题。
- **可选扩展**：解释修复发生在哪个抽象层。
- **证据**：源码 + 构建命令 + 测试输出 + 调试证据 + machine ID + Git commit。满足 FND-11 一个前置。

### FND-08 Transformer 推理与 KV Cache
- 类型 knowledge｜前置 FND-04
- **目标**：理解自回归推理中真正影响执行与内存的模型语义。
- **必须验收**：推导 Q/K/V/attention score/输出的 shape；解释 causal mask 与自回归生成；比较有/无 KV Cache 每步重复计算差异；解释 prefill 与 decode 的输入形态、并行度、资源特征；用伪代码或公式表达简化 Attention 与 KV Cache 更新；**外部锚点**——取一个真实开源模型 `config.json`，用其真实层数/head 数/KV head 数/hidden size 推导单层 Q/K/V 的 shape，并核对与实现或模型卡是否一致。
- **可选扩展**：列出该简化模型未覆盖的真实模型特性。
- **证据**：公式 + shape 推导 + 伪代码 + 时序图 + 外部锚点核对记录。解锁 FND-09，满足 FND-11 一个前置。

### FND-09 容量与性能模型
- 类型 knowledge｜前置 FND-02、FND-08
- **目标**：运行前能对显存/计算量/访存量/瓶颈做数量级预测。
- **必须验收**：据参数量与 dtype 估算权重显存；据层数/KV head/head dim/序列长度/batch/dtype 估算 KV Cache；对简化 Attention 或矩阵乘给出 FLOPs 与主要访存量估算；算 arithmetic intensity 并用 Roofline 思想形成可验证瓶颈假设；**外部锚点**——对一个真实开源模型用公开配置反推权重显存，与实际权重文件大小或官方模型卡显存要求对齐，误差落在可解释范围并说明来源。
- **可选扩展**：解释为何 prefill 更可能计算受限、decode 更可能内存带宽受限及例外；对估算误差来源作边界说明。
- **证据**：公式 + 计算表 + 至少两个手算样例 + 外部锚点核对记录。解锁 FND-10，满足 FND-11 一个前置。

### FND-10 基准方法与诊断设计
- 类型 knowledge｜前置 FND-09
- **目标**：能设计不会轻易产生误导结论的最小推理实验。
- **必须验收**：定义 TTFT/TPOT/ITL/端到端延迟/吞吐/错误率/P50-P95-P99；说明均值/中位数/高分位各自可能隐藏的问题；明确自变量/控制变量/预热/重复运行/原始数据格式；设计用于比较 batch、序列长度或 dtype 的单变量实验。
- **可选扩展**：针对 CPU/内存/IO/GPU 异常列出工具、指标及证据边界；明确 Linux 工具、PyTorch Profiler、GPU Profiler 各能回答什么。
- **证据**：实验协议 + 数据 schema + 诊断决策树。满足 FND-11 一个前置。

### FND-11 S1 集成门禁
- 类型 integration｜前置 FND-03、FND-05、FND-07、FND-08、FND-09、FND-10
- 能力 `linux_exec`、`pytorch_cpu`、`cpp_build`；`single_gpu` 可选增强，非强制。
- **前置检查**：六个前置任务全部为「完成」。
- **目标**：用一次真实执行把系统、tensor、模型与性能方法连成一体。
- **必须验收**：在实验机实现并测试简化 Attention 与增量 KV Cache；用自动化断言验证 shape/dtype/数值正确性；对至少两个输入长度、两个 batch 条件运行基准；保留每次原始结果而非只留汇总；把实测内存/耗时趋势与 FND-09 的预测比较，解释至少一个一致点与一个偏差；从新终端按文档复现测试与基准；记录 machine ID/Git commit/环境/失败尝试；完成一次不依赖笔记的综合自测。
- **可选扩展**：给出从请求到 token 的完整链路图，标注当前能观测/不能观测的位置。
- **证据**：源码 + 测试 + 原始数据 + 分析报告 + 复现输出 + 综合自测。解锁 PLAN-S2。

## 7. 进度记录（轻量）

不用 YAML + 校验脚本。一份 Markdown 清单即可，人工维护：

```markdown
| 任务 | 状态 | 前置 | 证据链接 | 备注(未做的可选项) |
|---|---|---|---|---|
| FND-01 | 完成 | - | diagrams/inference-e2e.md | 未做"三个不能证明的假设" |
| FND-02 | 进行中 | FND-01 |  |  |
| FND-03 | 未开始 | FND-02 |  |  |
| ...    | ...  | ...   |  |  |
```

规则只有两条：① 前置未全「完成」，后继不得标为可执行；② 标「完成」必须有证据链接。其余靠自觉，不靠工具强制——个人项目里 11 个任务不值得为之建一套校验系统。

## 8. 阶段滚动展开规则

`PLAN-S2` 及后续 `PLAN-SN` 执行：① 汇总上一阶段证据、失败实验、仍不稳定的能力；② 确认当前机器能力标签；③ 选真实上游项目/模型/硬件/基线；④ 拆成不超过 12 个原子任务；⑤ 同时设计知识任务、真实实验任务与集成门禁；⑥ 把上一阶段能力嵌入新集成验收形成累计复验；⑦ 检查依赖图无环、无不可达任务、无无证据验收；⑧ 只展开紧邻的下一阶段。

## 9. 外部验证原则

S1 已通过 §3.3 引入最低限度外部锚点。从 S2 起逐步加入：复现官方 benchmark 或论文结果；与至少一个真实推理引擎对照；阅读并修改上游源码；提交可审查的 issue/复现/文档/测试/PR；保存 reviewer 反馈；解释一次有效优化与一次无效或退化的优化。作品集中的性能数字必须可复现、有基线、有环境说明，能回答"为什么有效"和"在什么边界下失效"。

## 10. 非目标

不以下列成果作为核心成功标准：功能繁多的管理后台；通用 CRUD 型 Mini AI Platform；只完成 Kubernetes 部署；没有性能证据的 Gateway；与真实引擎无联系的长期玩具框架；只会孤立写 Kernel 却不能解释端到端影响；只会调 Profiler 却不能形成和验证性能假设。Kubernetes、Gateway、可观测性与平台治理在需要支撑推理性能/可靠性/成本问题时才学，不与主线平行扩张。

## 11. 完成定义

整条路线完成不是"所有文档已阅读"，而是能够：① 从模型结构推导推理计算与内存行为；② 从请求级指标定位到 Runtime/通信/Kernel；③ 在单 GPU 与分布式环境设计可复现实验；④ 修改真实系统并证明正确性、收益与边界；⑤ 对延迟/吞吐/容量/可靠性/成本做有数据支持的权衡；⑥ 通过上游贡献和公开报告获得外部验证；⑦ 完整解释一次成功优化、一次失败优化和一个真实故障。达到后，`Distributed Inference Lab` 才是能力证据，而不仅是一个学习仓库。
